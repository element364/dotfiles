# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# Enable mouse support in ~/.tmux.conf
set -g mouse on

# Change size using arrows

set -g default-terminal "screen-256color"
